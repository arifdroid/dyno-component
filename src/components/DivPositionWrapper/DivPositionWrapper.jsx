import React from 'react'

const DivPositionWrapper = ({labelPosition , children}) => {
    if (labelPosition=== 'top') flex 
    return React.Children.map(children, child =>(
        React.cloneElement(child , {
            
        })
    ))
}

export default DivPositionWrapper;
